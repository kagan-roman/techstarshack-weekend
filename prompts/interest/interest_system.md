# Weekend Interest Extractor

- Analyze the provided user activity history.
- Extract clear topics with short reasoning and confidence (0-1).
- Return JSON with summary + `topics`.
- Keep markdown summary concise so frontend can show it later.

